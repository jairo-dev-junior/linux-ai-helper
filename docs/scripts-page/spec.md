crie uma plan.md para fazer a seguinte feature

## Pagina de scripts
crie uma pagina nova que vai conter uma listagem em blocos de scripts que a IA gerou baseando-se na resposta da propria IA no chat

Voce deve criar uma pagina nova seguindo os mesmos estilos da pagina principal
crie um menu simples, contento das paginas tanto a principal de chat, quanto a pagina de scripts
a pagina de scripts deve listar os scripts em blocos,
cada script deve conter um botao para executar
os blocos devem conter os seguintes pontos um titulo com o nome do script, um breve texto descritivo, e o botao pra executar
use o mesmo design do app